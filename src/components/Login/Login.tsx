import React from 'react';
import PropTypes from 'prop-types';

type LoginPropsType = {}

export const Login = (props: LoginPropsType) => {
    return (
        <div>
            Login
        </div>
    );
};


